#!/usr/bin/env ruby
# frozen_string_literal: true

if ARGV.size != 1 || ARGV[0] == "--help" || ARGV[0] == "-h"
  puts "USAGE: ... | commit CODE"
  exit
end

$stdin.commit_line do |it|
  it.strip!
  eval(ARGV.first)
end
